import classNames from 'classnames'
import './index.scss'

const Tab: React.FC = () => {
    return (
        <div className={classNames('tab')}>
            <a className={classNames('post-card')}>
                nihao1
            </a>
        </div>
    )
}

export default Tab